version: '3.3'
services:
    filebrowser:
        image: filebrowser/filebrowser
        container_name: 018e7ff1-5f06-7e21-be3d-08acbb6240ec
        entrypoint:
            [
                '/filebrowser',
                '-a',
                '0.0.0.0',
                '-p',
                '80',
                '-d',
                '/database/filebrowser.db'
            ]
        restart: always
        volumes:
            - 'data:/srv'
            - '/var/lib/docker/volumes:/srv/volumes'
            - 'db:/database'
            - 'config:/etc/filebrowser'
        networks:
            - home-station
        labels:
            home-station.enable: true
            home-station.app: '@github.com/home-station-org/apps:filebrowser'
            home-station.app-version: 0.1.0

volumes:
  data:
  db:
  config:
            
networks:
  home-station:
    external: true
